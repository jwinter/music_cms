# Install with: `pip install -r requirements.txt`

# Infrastructure
Django >= 1.8.11, < 1.10
Pillow >= 3.1.1
MySQL-python >= 1.2.5
#for PostgreSQL: psycopg2 >= 2.6.1
python-memcached == 1.57
raven >= 5.11.1

# CMS libraries
django-fluent-dashboard >= 0.6.1

# Support libs
django-admin-tools >= 0.7.2

# Debugging
django-debugtools >= 1.5.1
ipython >= 4.1.2
